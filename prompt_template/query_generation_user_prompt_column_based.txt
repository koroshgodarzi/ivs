### These are the column names you may need to generate the query, you should not use any table or column other than the ones below:
{schemas}

### DATABASE RELATIONSHIPS (JOIN KEYS):
{join_info}

### RETRIEVED DATABASE VALUES (Actual matches in database columns for search keywords):
{retrieved_values}

User's question:
{user_question}